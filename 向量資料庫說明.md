# 向量資料庫運作原理

## 系統架構

```mermaid
flowchart TB
      subgraph LOCAL[你的電腦]
          APP[你的應用程式]
          VDB[(向量資料庫)]
      end

      subgraph REMOTE[遠端 Server]
          LLM[LLM - Ollama]
          EMB[Embedding 模型]
      end

      subgraph DB[資料庫 Server]
          DATA[(公司資料庫)]
      end

      APP <--> VDB
      APP <-->|API| LLM
      APP <-->|API| EMB
      APP <-->|查詢| DATA
```

## RAG 完整流程

```mermaid
sequenceDiagram
      participant U as 使用者
      participant APP as 你的應用
      participant DB as 公司資料庫
      participant VDB as 向量資料庫
      participant LLM as 遠端 LLM

      Note over APP,DB: 事前準備：建立向量索引
      APP->>DB: 1. 撈資料
      APP->>LLM: 2. 呼叫 Embedding API
      LLM-->>APP: 3. 回傳向量
      APP->>VDB: 4. 存入向量資料庫

      Note over U,LLM: 使用者查詢
      U->>APP: 5. 問問題
      APP->>LLM: 6. 問題轉向量
      LLM-->>APP: 7. 回傳向量
      APP->>VDB: 8. 搜尋相似內容
      VDB-->>APP: 9. 回傳相關資料
      APP->>LLM: 10. 問題 加 相關資料
      LLM-->>APP: 11. 生成回答
      APP-->>U: 12. 回覆
```

---

## 1. 向量資料庫怎麼存放資料

`build_index.py:47-57` 存入資料的結構：

| 欄位 | 範例 | 說明 |
|------|------|------|
| `id` | `"abc123"` | MongoDB 的 `_id`，唯一識別碼 |
| `embedding` | `[0.12, -0.45, 0.78, ...]` | 文字轉成的向量（bge-m3 是 1024 維） |
| `document` | `"雞腿 A001 肉品 大成"` | 原始文字（品名+編號+類別+品牌） |
| `metadata` | `{ProductNo, ProductName, ...}` | 額外資訊，查詢時會一起回傳 |

---

## 2. documents 與 metadatas 的差別

| | documents | metadatas |
|---|-----------|-----------|
| **格式** | 純文字字串 | 結構化字典 |
| **用途** | 給 embedding 模型看的原始文字 | 給程式用的欄位資料 |
| **可搜尋** | 是（用來產生向量） | 否（只是附帶資料） |

### 範例

假設 MongoDB 有一筆產品：

```python
{
    "_id": "abc123",
    "ProductNo": "A001",
    "ProductName": "去骨雞腿排",
    "ProductCategory": "肉品",
    "ProductBrand": "大成"
}
```

存入 ChromaDB 時：

```python
# document（build_index.py:37 組出來的）
"去骨雞腿排 A001 肉品 大成"   # ← 一個字串，拼在一起

# metadata（build_index.py:51-56）
{
    "ProductNo": "A001",
    "ProductName": "去骨雞腿排",
    "ProductCategory": "肉品",
    "ProductBrand": "大成"
}
```

### 為什麼要分開？

- **document** - 用來做語意搜尋（用戶說「我要雞腿」→ 比對 document 的向量）
- **metadata** - 用來顯示結果（找到後，從 metadata 拿出結構化資料顯示給用戶看）

如果只存 document，就沒辦法單獨取出「ProductNo」或「ProductName」，只會拿到一整串文字。

---

## 3. 搜尋怎麼比對

**比對原理**：計算兩個向量的「夾角」(Cosine Similarity)，越接近（角度越小）= 語意越相似

```mermaid
flowchart TB
    A["用戶輸入: 我要雞腿"] --> B["bge-m3 Embedding"]
    B --> C["查詢向量: [0.15, -0.42, 0.80, ...]"]
    C --> D["向量相似度比對"]

    D --> E["雞腿 A001 → 相似度 0.92 ✓"]
    D --> F["雞胸肉 A002 → 相似度 0.78"]
    D --> G["豬排 B001 → 相似度 0.23"]

    E --> H["回傳前 5 筆最相似的"]
```

---

## 4. 回傳哪些資訊

`collection.query()` 回傳的結構：

```python
{
    'ids': [['abc123', 'def456', ...]],           # 產品 ID
    'distances': [[0.08, 0.22, ...]],             # 距離（越小越相似）
    'documents': [['雞腿 A001...', '雞胸肉...']],  # 原始文字
    'metadatas': [[                               # 你存的 metadata
        {
            'ProductNo': 'A001',
            'ProductName': '雞腿',
            'ProductCategory': '肉品',
            'ProductBrand': '大成'
        },
        {...}
    ]]
}
```

---

## 5. 完整流程圖

### 建立索引 (build_index.py)

```mermaid
flowchart TB
    subgraph MongoDB
        A["產品資料"]
    end

    subgraph 處理流程
        B["組合 document<br/>去骨雞腿排 A001 肉品 大成"]
        C["bge-m3 Embedding"]
        D["embedding 向量<br/>[0.12, -0.45, 0.78, ...]"]
    end

    subgraph ChromaDB
        E["儲存資料"]
        F["• id: abc123"]
        G["• embedding: 比對用"]
        H["• document: 原文備查"]
        I["• metadata: 結果顯示用"]
    end

    A --> B --> C --> D --> E
    E --- F
    E --- G
    E --- H
    E --- I
```

### 查詢流程 (rag_query.py)

```mermaid
flowchart TB
    A["用戶: 我要雞腿"] --> B["bge-m3 Embedding"]
    B --> C["查詢向量"]
    C --> D["向量比對<br/>找出最相似的 top 5"]
    D --> E["找到 id: abc123<br/>相似度最高"]
    E --> F["回傳該筆 metadata"]
    F --> G["ProductNo: A001<br/>ProductName: 去骨雞腿排<br/>..."]
    G --> H["顯示給用戶"]
```

---

## 總結

**embedding 負責「找」，metadata 負責「顯示」。**
